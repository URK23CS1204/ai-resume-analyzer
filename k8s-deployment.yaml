apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resume-app
  template:
    metadata:
      labels:
        app: resume-app
    spec:
      containers:
      - name: backend
        image: resume-analyzer-backend
        imagePullPolicy: Never   # 🔥 THIS LINE FIXES YOUR ISSUE
        ports:
        - containerPort: 3000